# Rust
/target
**/*.rs.bk
*.pdb

# Node / SvelteKit / Tauri (frontend — added when the shell is scaffolded)
node_modules
/.svelte-kit
/build
/dist
/src-tauri/target
/src-tauri/gen

# Generated TS bindings are committed on purpose (drift guard); nothing to ignore there.

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/*
!.vscode/extensions.json

# Claude Code — shared settings (.claude/settings.json) are committed; the
# per-developer .local override is not.
.claude/settings.local.json

# Local env
.env
.env.*
!.env.example

# cogitlog — internal dev-reasoning notes, kept local (not in the public repo).
.cogitlog/
COGITLOG

# Claude Code local dir
.claude/

# Generated WASM build of the engine (run `npm run build:wasm` to regenerate).
# ~3 MB binary, rebuilt from crates/wasm — not committed.
/src/lib/wasm/

# Local dev scratch (chrome-devtools web-check screenshots, etc.)
/.dev/
