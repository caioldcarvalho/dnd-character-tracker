[workspace]
# src-tauri is added once the shell is scaffolded.
members = ["crates/engine"]
resolver = "2"

[workspace.package]
edition = "2021"
license = "MIT"
authors = ["caio"]

[workspace.dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
